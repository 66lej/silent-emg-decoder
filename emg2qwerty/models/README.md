This directory originally included pretrained checkpoints and a character language model.
They are omitted from the public repository because they are large generated artifacts rather than source code.
Beam-search runs that depend on the LM binary will require users to regenerate or supply the LM file separately.
